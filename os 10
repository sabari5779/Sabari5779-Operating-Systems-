#include <stdio.h>
#include <sys/ipc.h>
#include <sys/shm.h>

int main() {
    int shmid = shmget(1234, 100, 0666 | IPC_CREAT);
    char *data = (char*) shmat(shmid, NULL, 0);

    printf("Write data: ");
    gets(data);

    printf("Data read: %s\n", data);

    shmdt(data);
    return 0;
}
